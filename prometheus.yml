global:
  scrape_interval: 15s # Temps entre chaque collecte de données

scrape_configs:
  - job_name: "fastapi-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["backend:8000"]   # nom du service Docker Compose (résolution DNS interne)