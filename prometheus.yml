global:
  scrape_interval: 15s # Temps entre chaque collecte de données

scrape_configs:
  - job_name: "fastapi-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["fastapi_backend:8000"] # "backend" correspond au nom du service dans Docker